# PUBLIC_INTERFACE
# React app environment variables (must start with REACT_APP_)
# Provide the base URL for the backend API (e.g., http://localhost:3001 or deployed backend URL)
REACT_APP_API_BASE_URL=http://localhost:3001
